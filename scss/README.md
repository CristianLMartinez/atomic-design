# SCSS

```sh
  npm init -y #yarn init -y
  npm install normalize-scss #yarn add normalize-scss
  npm install --dev stylelint stylelint-config-sass-guidelines stylelint-config-prettier stylelint-prettier prettier
```